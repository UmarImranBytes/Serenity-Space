// ignore_for_file: non_constant_identifier_names

import 'package:flutter/material.dart';

class AppBorderRadius {
  static BorderRadius BORDER_RADIUS_05 =
      const BorderRadius.all(Radius.circular(5));
  static BorderRadius BORDER_RADIUS_10 =
      const BorderRadius.all(Radius.circular(10));
        static BorderRadius BORDER_RADIUS_12 = const BorderRadius.only(
      topLeft: Radius.circular(20), topRight: Radius.circular(20));
  static BorderRadius BORDER_RADIUS_14 = const BorderRadius.only(
      bottomLeft: Radius.circular(20), bottomRight: Radius.circular(20));
  static BorderRadius BORDER_RADIUS_15 =
      const BorderRadius.all(Radius.circular(15));
  static BorderRadius BORDER_RADIUS_20 =
      const BorderRadius.all(Radius.circular(20));
  static BorderRadius BORDER_RADIUS_25 =
      const BorderRadius.all(Radius.circular(25));
  static BorderRadius BORDER_RADIUS_30 =
      const BorderRadius.all(Radius.circular(30));
  static BorderRadius BORDER_RADIUS_100 =
      const BorderRadius.all(Radius.circular(100));

}
